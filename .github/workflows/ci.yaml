name: "CI/CD"

on:
  push:
    branches:
      - main # (Change to master if you kept the old branch name)
  pull_request:
  workflow_dispatch:

jobs:
  # JOB 1: Run the tests in a matrix
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features: ["speckit", "claude", "copilot-cli", "sqlcmd", "dotnet-aspire", "devcontainers-cli"]
        baseImage:
          - debian:latest
          - ubuntu:latest
          - mcr.microsoft.com/devcontainers/base:ubuntu
          - mcr.microsoft.com/devcontainers/base:ubuntu-24.04
          - mcr.microsoft.com/devcontainers/typescript-node:4-24-trixie
          - mcr.microsoft.com/devcontainers/dotnet:dev-10.0-noble
    steps:
      - uses: actions/checkout@v4

      - name: "Install Dev Container CLI"
        run: npm install -g @devcontainers/cli

      - name: "Run tests for '${{ matrix.features }}' against '${{ matrix.baseImage }}'"
        run: devcontainer features test -f ${{ matrix.features }} -i ${{ matrix.baseImage }} --skip-scenarios

  # JOB 2: Test dotnet-tools against .NET-enabled base images (full scenario matrix)
  test-dotnet-tools:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        baseImage:
          - mcr.microsoft.com/devcontainers/dotnet:1-9.0-bookworm
          - mcr.microsoft.com/devcontainers/dotnet:1-8.0-bookworm
          - mcr.microsoft.com/devcontainers/dotnet:dev-10.0-noble
    steps:
      - uses: actions/checkout@v4

      - name: "Install Dev Container CLI"
        run: npm install -g @devcontainers/cli

      - name: "Run tests for 'dotnet-tools' against '${{ matrix.baseImage }}'"
        run: devcontainer features test -f dotnet-tools -i ${{ matrix.baseImage }}

  # JOB 3: Publish the feature (ONLY if tests pass AND we are on the main branch)
  deploy:
    needs: [test, test-dotnet-tools] # <-- THIS IS THE MAGIC LINE
    if: ${{ github.ref == 'refs/heads/main' }} # (Change to master if needed)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: "Publish Features"
        uses: devcontainers/action@v1
        with:
          publish-features: "true"
          base-path-to-features: "./src"
          generate-docs: "true"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}