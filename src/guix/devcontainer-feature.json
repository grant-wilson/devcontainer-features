{
  "id": "guix",
  "version": "1.0.0",
  "name": "GNU Guix",
  "description": "Installs GNU Guix using the official binary installation script and configures optional channels, substitutes, and channel pinning.",
  "options": {
    "installNonguix": {
      "type": "boolean",
      "default": false,
      "description": "Add the nonguix channel with its recommended introduction metadata."
    },
    "extraChannels": {
      "type": "string",
      "default": "",
      "description": "Semicolon-separated channels in the form 'name|url|branch|introductionCommit|introductionFingerprint'."
    },
    "pinnedChannels": {
      "type": "string",
      "default": "",
      "description": "Comma-separated channel pins in the form 'channelName=commitHash'."
    },
    "authorizedSubstitutes": {
      "type": "string",
      "default": "",
      "description": "Semicolon-separated substitute definitions in the form 'url|keySource'. keySource may be an HTTPS URL, file path, or base64:<key>."
    },
    "installerUrl": {
      "type": "string",
      "default": "https://git.savannah.gnu.org/cgit/guix.git/plain/etc/guix-install.sh",
      "description": "Guix installer script URL."
    },
    "guileLoadPath": {
      "type": "string",
      "default": "",
      "description": "Colon-separated directories to prepend to GUILE_LOAD_PATH so Guile (.scm) modules can be imported from those locations, e.g. '/workspaces/myrepo/packages'."
    },
    "guileLoadCompiledPath": {
      "type": "string",
      "default": "",
      "description": "Colon-separated directories to prepend to GUILE_LOAD_COMPILED_PATH so compiled Guile (.go) modules can be loaded from those locations, e.g. '/workspaces/myrepo/packages'."
    }
  }
}
